
  # Smart Seed Zone Recommender

  This is a code bundle for Smart Seed Zone Recommender. The original project is available at https://www.figma.com/design/cDShyc3l1d0wPj8VRCAM0a/Smart-Seed-Zone-Recommender.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  